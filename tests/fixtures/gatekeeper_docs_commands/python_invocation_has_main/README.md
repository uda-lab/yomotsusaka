# Drift: python_invocation_has_main

```sh
uv run python -m yomotsusaka.pipeline
```

(`yomotsusaka.pipeline` is importable but exposes no `main()` /
`__main__` block — the F1 silent-no-op precedent that motivated B1.)
