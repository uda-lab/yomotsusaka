# Drift: fixture_path_seeded

This is a NEW doc (not README/AGENTS.md), so the canonical-quickstart
whitelist does not apply.

```sh
uv run python -m yomotsusaka.cli.run_batch ./inbox --vault-root ./vault
```
