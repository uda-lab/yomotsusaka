# Vocab drift — category fixture

This doc references a non-canonical OperationalCategory token.

The runner emits `batch_committed` when all inbox files commit,
but `batch_committed` is not a member of the canonical enum.
