"""Command-line entry points for ``yomotsusaka``.

Currently exposes only the batch-runner entry at
:mod:`yomotsusaka.cli.run_batch`. Invocation is via ``python -m`` only; no
console-script entry is registered in ``pyproject.toml`` per the child_06
spec (keeps the project surface minimal).
"""
