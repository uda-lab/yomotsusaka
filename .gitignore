# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# uv
.uv/

# Vault (local dev only — never commit private data)
.vault/

# RunPod lifecycle log written by scripts/manage_runpod.py
# (the per-user file under ~/.cache/yomotsusaka/lifecycle.jsonl is
# already outside the repo, but a test or a misconfigured run could
# write a copy to the working directory — ensure it stays untracked).
lifecycle.jsonl

# Environment files (never commit credentials; see .env.example)
.env
.env.local
.env.*.local

# Pytest / coverage
.pytest_cache/
.coverage
htmlcov/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
